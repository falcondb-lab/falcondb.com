[package]
name = "order_writer"
version = "0.1.0"
edition = "2021"
description = "FalconDB DCG PoC — Deterministic order writer"

[dependencies]
postgres = "0.19"
chrono = "0.4"
serde = { version = "1", features = ["derive"] }
serde_json = "1"
