[workspace]

[package]
name = "oltp_writer"
version = "0.1.0"
edition = "2021"

[dependencies]
postgres = "0.19"
rand = "0.8"
clap = { version = "4", features = ["derive"] }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
chrono = "0.4"
